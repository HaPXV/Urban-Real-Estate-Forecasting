model: arima
params:
  # ARIMA(3,1,3) selected via grid-search on training set using AIC/BIC
  # and validated by out-of-sample MAPE (paper Section V.A)
  order: [3, 1, 3]
  seasonal_order: [0, 0, 0, 0]
